<!-- Main jumbotron for a primary marketing message or call to action -->
<div class="bg-white p-3 mb-3">
  <div class="container">
    <h1 class="display-4 mb-3">Pregunta y Responde</h1>
    <p>
      El mejor lugar para aprender y ayudar a quienes están aprendiendo, crea preguntas para resolver
      tus dudas y comparte tu conocimiento respondiendo si conoces del tema
    </p>
  </div>
</div>

<div class="container">
  <!-- Example row of columns -->
  <h2 class="mb-4">Últimas preguntas</h2>
  {{#with error}}
  <div class="container text-center">
    <div class="alert alert-danger">
      <strong>{{this}}</strong>
    </div>
  </div>
  {{/with}}
  {{#if questions}}
  {{#each questions as |question key|}}
  <div class="card bg-white mb-3">
    <div class="card-body">
      <div class="row">
        <div class="col-12 col-md-2 text-center d-none d-sm-block">
          <a href="/question/{{key}}" class="text-dark">
            <h3>
              {{#if question.answers }}
              {{numAnswers question.answers}}
              {{else}}
              0
              {{/if}}
            </h3>
            Respuestas
          </a>
        </div>
        <div class="col-12 col-md-10 d-flex flex-column">
          <h4 class="mt-3">
            <a href="/question?id={{key}}" class="text-dark">{{question.title}}</a>
          </h4>
          <p>Autor: {{question.owner.name}}</p>
        </div>
        <div class="col-12 col-md-2 text-left d-block d-sm-none">
          <a href="/question/{{key}}" class="text-dark">
            <h2>
              {{#if question.answers }}
              {{numAnswers question.answers}}
              {{else}}
              0
              {{/if}}
            </h2>
            Respuestas
          </a>
        </div>
      </div>
    </div>
  </div>
  <hr>
  {{/each}}
  {{/if}}
</div> <!-- /container -->